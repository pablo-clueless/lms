import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder
      title="Examinations"
      description="Access examinations during the scheduled window and view results."
    />
  );
};

export default Page;
