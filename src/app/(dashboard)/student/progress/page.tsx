import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder title="Progress" description="Track your academic progress and view grades across all courses." />
  );
};

export default Page;
