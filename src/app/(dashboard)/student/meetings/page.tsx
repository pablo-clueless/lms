import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Meetings" description="Join virtual meetings scheduled for your class." />;
};

export default Page;
