import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Assignments" description="View and submit your assignments before the deadline." />;
};

export default Page;
