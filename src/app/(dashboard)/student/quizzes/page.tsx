import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Quizzes" description="Take quizzes and view your quiz results." />;
};

export default Page;
