import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Tutors" description="Invite, manage, and assign tutors to courses." />;
};

export default Page;
