import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Timetables" description="Generate, publish, and manage class timetables." />;
};

export default Page;
