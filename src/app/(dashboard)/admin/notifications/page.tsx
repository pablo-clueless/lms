import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Notifications" description="View notifications and send announcements to users." />;
};

export default Page;
