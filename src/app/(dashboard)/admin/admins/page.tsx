import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Admins" description="Manage school administrators and their permissions." />;
};

export default Page;
