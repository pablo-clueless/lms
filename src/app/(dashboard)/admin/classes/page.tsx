import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder title="Classes" description="Create and manage classes (JSS1, Primary 5, etc.) within sessions." />
  );
};

export default Page;
