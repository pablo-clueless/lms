import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Assessments" description="Overview of all quizzes, assignments, and examinations." />;
};

export default Page;
