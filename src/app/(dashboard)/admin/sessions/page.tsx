import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Sessions" description="Manage academic sessions (school years) for your school." />;
};

export default Page;
