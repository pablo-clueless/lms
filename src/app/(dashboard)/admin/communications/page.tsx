import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Communications" description="Send broadcast emails and messages to users." />;
};

export default Page;
