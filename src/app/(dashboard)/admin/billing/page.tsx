import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Billing" description="View invoices, payment history, and subscription details." />;
};

export default Page;
