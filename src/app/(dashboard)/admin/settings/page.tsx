import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Settings" description="Configure school settings, branding, and preferences." />;
};

export default Page;
