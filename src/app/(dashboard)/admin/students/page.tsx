import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder title="Students" description="Enroll students, manage enrollments, and track student data." />
  );
};

export default Page;
