import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Terms" description="Configure term dates, holidays, and non-instructional days." />;
};

export default Page;
