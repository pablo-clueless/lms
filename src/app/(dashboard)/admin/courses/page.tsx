import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Courses" description="Create courses and assign tutors to each class." />;
};

export default Page;
