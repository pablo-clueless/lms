import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Reports" description="Generate and export report cards and school-wide reports." />;
};

export default Page;
