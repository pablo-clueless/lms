import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Progress" description="View student progress across all classes and courses." />;
};

export default Page;
