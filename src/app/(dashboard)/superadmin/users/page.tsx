import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Users" description="View and manage users across all tenants." />;
};

export default Page;
