import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder title="Permissions" description="Manage role-based access control and permission templates." />
  );
};

export default Page;
