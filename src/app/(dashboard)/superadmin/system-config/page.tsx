import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder title="System Config" description="Configure platform-wide settings, integrations, and pricing." />
  );
};

export default Page;
