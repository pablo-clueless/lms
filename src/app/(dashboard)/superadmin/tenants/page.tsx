import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Tenants" description="Onboard, manage, suspend, and reactivate school tenants." />;
};

export default Page;
