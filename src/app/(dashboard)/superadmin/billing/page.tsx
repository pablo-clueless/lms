import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder
      title="Billing"
      description="View aggregated billing, invoices, and payment status across all tenants."
    />
  );
};

export default Page;
