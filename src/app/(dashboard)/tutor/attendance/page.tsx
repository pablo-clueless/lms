import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Attendance" description="Mark and track student attendance for each period." />;
};

export default Page;
