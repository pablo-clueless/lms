import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Courses" description="Manage your assigned courses and course materials." />;
};

export default Page;
