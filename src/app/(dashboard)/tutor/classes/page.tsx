import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="My Classes" description="View and manage the classes you are assigned to teach." />;
};

export default Page;
