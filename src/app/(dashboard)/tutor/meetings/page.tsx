import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Meetings" description="Schedule and manage virtual meetings for your classes." />;
};

export default Page;
