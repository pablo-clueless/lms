import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Students" description="View students in your assigned classes and their progress." />;
};

export default Page;
