import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Quizzes" description="Create and manage quizzes for your courses." />;
};

export default Page;
