import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return (
    <PagePlaceholder
      title="Examinations"
      description="Create end-of-term examinations and grade student submissions."
    />
  );
};

export default Page;
