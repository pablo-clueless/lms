import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Communications" description="Send emails and messages to students in your classes." />;
};

export default Page;
