import { PagePlaceholder } from "@/components/shared";

const Page = () => {
  return <PagePlaceholder title="Notifications" description="View notifications and important updates." />;
};

export default Page;
